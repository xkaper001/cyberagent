[pytest]
# Repo root on sys.path so `import backend` works without an editable install.
pythonpath = .
markers =
    live_llm: needs a reachable LLM endpoint and Docker workers; deselected in CI with -m "not live_llm"
