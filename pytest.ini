[pytest]
# Repo root on sys.path so `import backend` works without an editable install.
pythonpath = .
markers =
    live_llm: needs a reachable LLM endpoint; deselected in CI
    needs_worker: actually runs nmap in a Docker worker; deselected in CI
